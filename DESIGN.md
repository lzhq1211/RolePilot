# RolePilot 设计规范入口

RolePilot Web 的 UI 规范唯一事实源是 [`docs/design/BIONOVA.md`](./docs/design/BIONOVA.md)（BIONOVA 前端规范 v1.0，白底、深灰文字、柔蓝药丸、Top Pill 与 token 体系）。

历史来源说明：本规范最初按仓库外的 `../FRONTEND_HANDOVER_SPEC.md` 同步生成，该外部文件自 2026-09-08 起仅作历史来源，不再作为执行依据。排版器冻结边界见 `packages/resume-workbench`（`resume.css`、`renderer.js`、A4 纸张 DOM、`print.css` 不改）。
